# WeWood Homepage

Static homepage prototype for WeWood, built as a clone of the Noom (Boutique) Shopify theme's homepage structure, grounded in WeWood's real live content (categories, products, prices, trust signals, contact info).

Deployed automatically to GitHub Pages via the workflow in `.github/workflows/deploy.yml` on every push to `main`.
